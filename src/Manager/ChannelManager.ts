import BaseChannel from "../DiscordTypes/Base/BaseChannel";
import { BaseManager } from "./BaseManager";

export class ChannelManager extends BaseManager<BaseChannel> {
    public async fetch(id: string){
        
    }
}
